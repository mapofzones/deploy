apiVersion: apps/v1
kind: Deployment
metadata:
  name: gravitybridge-watcher-deploy
  labels:
    app: gravitybridge-watcher-app
  annotations:
spec:
  selector:
    matchLabels:
      app: gravitybridge-watcher-app

  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 2
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: gravitybridge-watcher-app
    spec:
      affinity:
        podAntiAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
          - labelSelector:
              matchExpressions:
              - key: app
                operator: In
                values:
                - gravitybridge-watcher-app
            topologyKey: "kubernetes.io/hostname"
      hostAliases:
      - ip: "159.89.250.255"
        hostnames:
        - "api.mapofzones.com"
      containers:

      - name: gravity-bridge-3
        image: registry.digitalocean.com/mapofzones/cosmos-watcher-gravity-bridge:2f5fc61b
        env:
          - name: chain_id
            value: "gravity-bridge-3"
          - name: rabbitmq
            valueFrom:
              secretKeyRef:
                name: prodsecret
                key: watcher_rabbitmq
          - name: graphql
            valueFrom:
              secretKeyRef:
                name: prodsecret
                key: db_graphql
        imagePullPolicy: Always
        resources:
          requests:
            memory: "150Mi"
            cpu: "30m"
          limits:
            memory: "256Mi"
            cpu: "250m"
