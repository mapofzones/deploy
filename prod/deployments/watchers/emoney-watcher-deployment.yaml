apiVersion: apps/v1
kind: Deployment
metadata:
  name: emoney-watcher-deploy
  labels:
    app: emoney-watcher-app
  annotations:
spec:
  selector:
    matchLabels:
      app: emoney-watcher-app

  replicas: 3
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 2
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: emoney-watcher-app
    spec:
      hostAliases:
      - ip: "159.89.250.255"
        hostnames:
        - "api.mapofzones.com"
      containers:

      - name: emoney-3
        image: registry.digitalocean.com/mapofzones/cosmos-watcher-emoney:c43c6dc7
        env:
          - name: chain_id
            value: "emoney-3"
          - name: rabbitmq
            valueFrom:
              secretKeyRef:
                name: prodsecret
                key: watcher_rabbitmq
          - name: graphql
            valueFrom:
              secretKeyRef:
                name: prodsecret
                key: db_graphql
        imagePullPolicy: Always
        resources:
          requests:
            memory: "150Mi"
            cpu: "30m"
          limits:
            memory: "256Mi"
            cpu: "250m"
