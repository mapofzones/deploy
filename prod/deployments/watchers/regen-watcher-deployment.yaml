apiVersion: apps/v1
kind: Deployment
metadata:
  name: regen-watcher-deploy
  labels:
    app: regen-watcher-app
  annotations:
spec:
  selector:
    matchLabels:
      app: regen-watcher-app

  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 2
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: regen-watcher-app
    spec:
      hostAliases:
      - ip: "159.89.250.255"
        hostnames:
        - "api.mapofzones.com"
      containers:

      - name: regen-1
        image: registry.digitalocean.com/mapofzones/cosmos-watcher-regen:4d07b8d2
        env:
          - name: chain_id
            value: "regen-1"
          - name: rabbitmq
            valueFrom:
              secretKeyRef:
                name: prodsecret
                key: watcher_rabbitmq
          - name: graphql
            valueFrom:
              secretKeyRef:
                name: prodsecret
                key: db_graphql
        imagePullPolicy: Always
        resources:
          requests:
            memory: "150Mi"
            cpu: "30m"
          limits:
            memory: "256Mi"
            cpu: "250m"
