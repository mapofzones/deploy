apiVersion: apps/v1
kind: Deployment
metadata:
  name: impacthub-watcher-deploy
  labels:
    app: impacthub-watcher-app
  annotations:
spec:
  selector:
    matchLabels:
      app: impacthub-watcher-app

  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 2
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: impacthub-watcher-app
    spec:
      hostAliases:
      - ip: "159.89.250.255"
        hostnames:
        - "api.mapofzones.com"
      containers:

      - name: impacthub-3
        image: registry.digitalocean.com/mapofzones/cosmos-watcher-impacthub:265381f2
        env:
          - name: chain_id
            value: "impacthub-3"
          - name: rabbitmq
            valueFrom:
              secretKeyRef:
                name: prodsecret
                key: watcher_rabbitmq
          - name: graphql
            valueFrom:
              secretKeyRef:
                name: prodsecret
                key: db_graphql
        imagePullPolicy: Always
        resources:
          requests:
            memory: "150Mi"
            cpu: "30m"
          limits:
            memory: "256Mi"
            cpu: "250m"
